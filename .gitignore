# Dependencies
web/node_modules/
node_modules/

# Next.js
.next/
web/.next/
web/out/

# Production
web/build/
web/.next/standalone/

# OpenNext / Cloudflare Workers build artifacts
web/.open-next/
.open-next/

# Environment variables
.env
.env.local
.env.*.local
web/.env
web/.env.local
web/.env.*.local

# Filled-in launch questionnaire — contains real secrets and personal data
LAUNCH_READINESS_QUESTIONNAIRE.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
web/coverage/
web/test-results/
web/playwright-report/
web/blob-report/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo
web/next-env.d.ts

# Docker
.docker/

# Misc
*.log
.vercel

# Vercel / Cloudflare state (not deploy config — those are tracked)
.vercel/
web/.vercel/
.wrangler/
web/.wrangler/
start-local.sh

# Launch-readiness questionnaire (filled, contains real contact data)
LAUNCH_READINESS_QUESTIONNAIRE.md

# Claude Code isolated agent worktrees — ephemeral, never commit
# (tracking these as gitlinks without .gitmodules breaks Cloudflare Pages)
.claude/worktrees/

# Tenant images copied into public/ by web/scripts/copy-tenant-images.mjs
# on every prebuild. Source of truth lives at sites/<slug>/images/;
# web/public/sites/ is a regenerated build artifact, never committed.
web/public/sites/
