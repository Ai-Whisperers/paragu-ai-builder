# Production Dockerfile for Hostinger VPS
FROM node:20-bookworm

WORKDIR /app

# Copy everything from web directory
COPY web ./web

WORKDIR /app/web

ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME=0.0.0.0

EXPOSE 3000

CMD ["node", "server.js"]